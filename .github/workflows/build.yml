name: Build iOS TVBox Release (TrollStore专用无签名版)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-ios-trollstore-unsigned:
    runs-on: macos-14
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: 拉取Git LFS大文件（NodeMobile框架）
        run: git lfs pull

      - name: 锁定Xcode稳定兼容版本
        run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable
          cache: true

      - name: 全量清理构建缓存
        run: |
          flutter clean
          rm -rf ios/Pods ios/Podfile.lock ios/build .dart_tool build
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          rm -rf ~/Library/Developer/Xcode/ModuleCache.noindex

      - name: 把xcframework拆成普通真机framework，彻底绕开Xcode解压路径问题
        run: |
          # 把真机的framework从xcframework里拆出来，直接用普通framework
          cp -r ios/Frameworks/NodeMobile.xcframework/ios-arm64/NodeMobile.framework ios/Frameworks/
          # 删除原来的xcframework，避免Xcode自动识别解压
          rm -rf ios/Frameworks/NodeMobile.xcframework
          # 动态修改NodeMobile的podspec，把vendored_frameworks从xcframework改成普通framework
          sed -i '' 's/NodeMobile.xcframework/NodeMobile.framework/' ios/Frameworks/NodeMobile.podspec
          # 列出所有头文件，看看都有啥
          ls -la ios/Frameworks/NodeMobile.framework/
          ls -la ios/Frameworks/NodeMobile.framework/*/
          # 直接修改桥接头，导入NodeJSBridge自己的头文件，绕开NodeMobile那个依赖模块的伞头
          sed -i '' 's/#import.*/#import \"..\/Frameworks\/NodeMobile.framework\/PrivateHeaders\/NodeJSBridge.h\"/' ios/Runner/Runner-Bridging-Header.h
          echo "✅ 已将NodeMobile从xcframework转为普通真机framework，所有路径已对齐"

      - name: 预检查（Dart静态分析）
        run: |
          flutter pub get
          flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: 验证所有文件
        run: |
          # 验证NodeJSBridge头文件
          HEADER_PATH="ios/Frameworks/NodeMobile.framework/PrivateHeaders/NodeJSBridge.h"
          if [ -f "$HEADER_PATH" ]; then
            echo "✅ NodeJSBridge头文件位置正确: $HEADER_PATH"
            cat "$HEADER_PATH"
          else
            echo "❌ NodeJSBridge头文件未找到"
            ls -laR ios/Frameworks/NodeMobile.framework/
            exit 1
          fi
          
          # 验证桥接头文件已经修改正确
          BRIDGE_PATH="ios/Runner/Runner-Bridging-Header.h"
          if grep -q "NodeJSBridge.h" "$BRIDGE_PATH"; then
            echo "✅ 桥接头文件已修改为直接导入NodeJSBridge"
            cat "$BRIDGE_PATH"
          else
            echo "❌ 桥接头文件未修改"
            cat "$BRIDGE_PATH"
            exit 1
          fi

      - name: 重新安装iOS依赖
        run: |
          flutter pub get
          cd ios
          pod deintegrate
          pod install --repo-update
          cd ..

      - name: 无签名构建（TrollStore专用）
        run: |
          flutter build ios --release --no-codesign --config-only
          cd ios
          xcodebuild -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            clean archive \
            -archivePath $PWD/build/Runner.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=NO
          cp -R build/Runner.xcarchive/Products/Applications/Runner.app ../build/ios/iphoneos/
          cd ..

      - name: 校验构建产物
        run: |
          if [ -d "build/ios/iphoneos/Runner.app" ]; then
            echo "✅ 无签名APP构建成功"
            ls -la build/ios/iphoneos/Runner.app
          else
            echo "❌ 构建失败：未找到 Runner.app"
            exit 1
          fi

      - name: 打包 IPA（TrollStore 专用）
        run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r tvbox_trollstore_unsigned.ipa Payload/
          rm -rf Payload/

      - name: 校验 IPA 生成
        run: |
          if [ -f "tvbox_trollstore_unsigned.ipa" ]; then
            echo "✅ IPA 生成成功"
            ls -la tvbox_trollstore_unsigned.ipa
          else
            echo "❌ IPA 生成失败"
            exit 1
          fi

      - name: 上传无签名 IPA 产物
        uses: actions/upload-artifact@v4
        with:
          name: tvbox_trollstore_unsigned_ipa
          path: tvbox_trollstore_unsigned.ipa
          retention-days: 30
