name: Build iOS TVBox
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 检查所有LFS指针文件
        run: |
          # 找到所有framework里的二进制文件，检查它们是不是LFS指针
          find ios/Frameworks -name "*.framework" -type d | while read fw; do
            bin_name=$(basename $fw .framework)
            bin_path="$fw/$bin_name"
            if [ -f "$bin_path" ]; then
              echo "检查 $bin_path..."
              # 检查是不是LFS指针
              if head -1 "$bin_path" | grep -q "oid sha256:"; then
                echo "  ✅ 这是LFS指针文件，添加到.gitattributes"
                echo "$bin_path filter=lfs diff=lfs merge=lfs -text" >> .gitattributes
              fi
            fi
          done
          cat .gitattributes

      - name: 拉取所有LFS大文件
        run: |
          # 现在拉取所有的LFS对象
          git lfs fetch --all
          git lfs checkout
          git lfs status

      - name: 安装Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable
          cache: true

      - name: 清理缓存
        run: |
          flutter clean
          rm -rf ios/Pods ios/Podfile.lock ios/build .dart_tool build
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          rm -rf ~/Library/Developer/Xcode/ModuleCache.noindex

      - name: 获取Dart依赖
        run: flutter pub get

      - name: 静态检查
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: 拆解XCFramework为普通Framework
        run: |
          # 把XCFramework里的arm64版本直接提取出来，避免Xcode自动解压导致的路径变化
          cp -R ios/Frameworks/NodeMobile.xcframework/ios-arm64/NodeMobile.framework ios/Frameworks/
          echo "✅ 已拆解XCFramework为普通Framework"

      - name: 检查所有二进制文件
        run: |
          echo "✅ 检查所有Framework二进制文件:"
          find ios/Frameworks -name "*.framework" -type d | while read fw; do
            bin_name=$(basename $fw .framework)
            bin_path="$fw/$bin_name"
            if [ -f "$bin_path" ]; then
              if head -1 "$bin_path" | grep -q "oid sha256:"; then
                echo "❌ $bin_path 还是LFS指针，未拉取成功！"
              else
                echo "✅ $bin_path 二进制文件正常，大小: $(du -h $bin_path | cut -f1)"
                file $bin_path
              fi
            fi
          done

      - name: 生成自定义桥接头
        run: |
          # 手动写入NodeJSBridge的接口，让Swift能识别到静态方法
          echo "#ifndef Runner_Bridging_Header_h" > ios/Runner/Runner-Bridging-Header.h
          echo "#define Runner_Bridging_Header_h" >> ios/Runner/Runner-Bridging-Header.h
          echo "" >> ios/Runner/Runner-Bridging-Header.h
          echo "#import \"GeneratedPluginRegistrant.h\"" >> ios/Runner/Runner-Bridging-Header.h
          echo "#import <Flutter/Flutter.h>" >> ios/Runner/Runner-Bridging-Header.h
          echo "" >> ios/Runner/Runner-Bridging-Header.h
          echo "@interface NodeJSBridge : NSObject" >> ios/Runner/Runner-Bridging-Header.h
          echo "+ (void)registerWithRegistrar:(NSObject<FlutterPluginRegistrar> *)registrar;" >> ios/Runner/Runner-Bridging-Header.h
          echo "@end" >> ios/Runner/Runner-Bridging-Header.h
          echo "" >> ios/Runner/Runner-Bridging-Header.h
          echo "#endif" >> ios/Runner/Runner-Bridging-Header.h

      - name: 重新安装iOS依赖
        run: |
          flutter pub get
          cd ios
          pod deintegrate
          pod install --repo-update

      - name: 无签名构建
        run: |
          flutter build ios --release --no-codesign --config-only
          cd ios
          xcodebuild -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            clean archive \
            -archivePath $PWD/build/Runner.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=NO
          cp -R build/Runner.xcarchive/Products/Applications/Runner.app ../build/ios/iphoneos/

      - name: 打包IPA
        run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r tvbox_trollstore_unsigned.ipa Payload/
          rm -rf Payload/

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: tvbox_trollstore_unsigned_ipa
          path: tvbox_trollstore_unsigned.ipa
          retention-days: 30
